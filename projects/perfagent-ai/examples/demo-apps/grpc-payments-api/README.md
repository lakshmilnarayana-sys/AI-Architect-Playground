# gRPC Payments Demo

Simple unary gRPC service for future gRPC load generation.

Run:

```bash
docker compose --profile demo up --build demo-grpc-payments
```

Service: `payments.Payments/CreatePayment` on `localhost:8082`.
