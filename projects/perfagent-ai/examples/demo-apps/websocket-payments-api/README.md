# WebSocket Payments Demo

Echo-style payment authorization service for future WebSocket load generation.

Run:

```bash
docker compose --profile demo up --build demo-websocket-payments
```

Endpoint: `ws://localhost:8081`
