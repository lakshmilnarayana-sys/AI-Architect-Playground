# Runtime dependencies for Streamlit Community Cloud.
# Docling is intentionally not installed at runtime; ingestion output is committed
# at data/processed/chunks.json. Use requirements-ingest.txt for offline ingestion.
streamlit>=1.40
qdrant-client>=1.12
fastembed>=0.7
openai>=1.109
pandas>=2.2
